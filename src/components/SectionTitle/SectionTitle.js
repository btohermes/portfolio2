import React from 'react';

import classes from './SectionTitle.css';

const ComponentName = props => {
  return <div className={classes.SectionTitle}>{props.Title}</div>;
};

export default ComponentName;
