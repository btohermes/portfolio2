import React from 'react';

//The object inside createContext can be null but for the current propose this ways is given better autocompletion
const Context = React.createContext();

export default Context;
